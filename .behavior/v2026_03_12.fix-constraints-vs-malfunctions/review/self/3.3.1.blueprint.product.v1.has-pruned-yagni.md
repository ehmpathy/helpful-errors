# self-review: has-pruned-yagni

## reviewed artifact
- `.behavior/v2026_03_12.fix-constraints-vs-malfunctions/3.3.1.blueprint.product.v1.i1.md`

---

## YAGNI analysis: component by component

### 1. ConstraintError class

| question | answer |
|----------|--------|
| explicitly requested in wish? | **YES** — "lets release a ConstraintError" |
| minimum viable? | **YES** — extends BadRequestError, inherits behavior |
| abstraction for future? | **NO** — concrete class |
| feature creep? | **NO** — only what wish asks |

**verdict:** keep.

---

### 2. MalfunctionError class

| question | answer |
|----------|--------|
| explicitly requested in wish? | **YES** — "lets release a MalfunctionError" |
| minimum viable? | **YES** — extends UnexpectedCodePathError, inherits behavior |
| abstraction for future? | **NO** — concrete class |
| feature creep? | **NO** — only what wish asks |

**verdict:** keep.

---

### 3. HelpfulErrorCode.exit field

| question | answer |
|----------|--------|
| explicitly requested? | **YES** — wish says "exit.code = 2" and "exit.code = 1" |
| minimum viable? | **YES** — single optional field |
| premature? | **NO** — wish explicitly specifies exit codes |

**verdict:** keep.

---

### 4. static emoji property

| question | answer |
|----------|--------|
| explicitly requested? | **YES** — wish says "emoji = ✋" and "emoji = 💥" |
| minimum viable? | **YES** — single string constant |
| could be simpler? | **MAYBE** — could document-only without code |

**analysis:**
wish says `emoji = ✋` but doesn't specify implementation.
options:
1. code: `static emoji = '✋'`
2. docs-only: "use ✋ for ConstraintError in your logs"

the vision decided: static property gives programmatic access for log utilities.
this is a design decision, not feature creep.

**verdict:** keep — vision justified this.

---

### 5. dynamic prefix via this.constructor.name

| question | answer |
|----------|--------|
| explicitly requested? | **NO** — not in wish |
| minimum viable? | **YES** — small change |
| premature optimization? | **NO** — required |
| while we're here? | **NEEDS SCRUTINY** |

**deep dive: is this truly required?**

the wish says:
- ConstraintError extends BadRequestError
- ConstraintError messages should identify themselves

without dynamic prefix:
```ts
new ConstraintError('invalid email')
// → message: "BadRequestError: invalid email"  ← WRONG
```

with dynamic prefix:
```ts
new ConstraintError('invalid email')
// → message: "ConstraintError: invalid email"  ← CORRECT
```

**alternative approaches:**

| approach | code | outcome |
|----------|------|---------|
| dynamic prefix in parent | `this.constructor.name` | works, minimal change |
| override prefix in child | double prefix problem | "ConstraintError: BadRequestError: msg" |
| skip parent constructor | impossible in JS | n/a |
| don't extend parent | breaks instanceof | violates wish |
| accept wrong prefix | confuses users | violates spirit of wish |

**conclusion:** dynamic prefix is NOT YAGNI — it's the minimum solution to satisfy:
1. ConstraintError extends BadRequestError (wish requirement)
2. ConstraintError messages say "ConstraintError:" (implicit requirement)

**verdict:** keep — required for wish fulfillment.

---

### 6. README documentation

| question | answer |
|----------|--------|
| explicitly requested? | **YES** — user said "we'll want to update the readme" |
| minimum viable? | **YES** — document new classes |

**verdict:** keep.

---

### 7. test files

| question | answer |
|----------|--------|
| explicitly requested? | implied — all code needs tests |
| minimum viable? | **YES** — mirror extant test patterns |

**verdict:** keep.

---

## extras found: NONE

every component in the blueprint is either:
1. explicitly requested in the wish
2. required to fulfill the wish
3. requested by the user (README)

no YAGNI violations detected.

---

## simplification check

| component | could be simpler? |
|-----------|-------------------|
| ConstraintError | no — already minimal |
| MalfunctionError | no — already minimal |
| exit field | no — single field |
| emoji | maybe docs-only, but vision justified code |
| dynamic prefix | no — required for wish |
| README | no — document what's added |
| tests | no — follow extant patterns |

---

## conclusion

**no YAGNI violations.** all components are either:
- explicitly requested
- required for wish fulfillment
- justified by vision decisions
