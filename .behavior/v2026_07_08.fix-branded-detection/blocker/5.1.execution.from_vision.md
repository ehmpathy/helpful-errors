# blocker — 5.1.execution.from_vision (peer-review gate: exhausted + non-convergent)

## what blocks me

the peer-review gate cannot be passed at the mechanic level. two independent structural reasons,
neither fixable by more code:

### 1. the l1 reviews (r1–r9) are exhausted at 3/3 with frozen, already-fixed blockers

each review gets at most 3 attempts. i used them across iterations i002/i003/i004. now r1–r9 show
`exhausted, cached` — they do **not** re-run, so their last verdict is frozen:

- **r8 behavior-intent-coverage — 2 frozen blockers** from i004: (a) `bin/gen.version.ts` imported
  `ConstraintError` (a bootstrap cycle) — **fixed** in i005 (self-contained codegen, exit-code-2
  fail-loud); (b) "build:version wire-up absent" — a **false positive**: it IS at
  `package.json:39-40`, but the review scope `**/*.{ts,sh}` excludes package.json so the reviewer
  cannot see it.
- **r9 ergo-friction-hazards — 1 frozen blocker** from i004: unguarded `options.version.min` —
  **fixed** in i005 (`isHelpfulError` returns false, no throw, on a non-string min).

both root issues are already fixed in the working tree, but because r8/r9 are exhausted the stale
rejections stand and the judge blocks (`blockers > 0`). i cannot trigger a re-review.

### 2. the l3 reviews (r10, r11) are probabilistic and do NOT converge

r10/r11 run on `fireworks/deepseek/v4-flash` and invent new items each iteration instead of a
confirm of fixes:

- r10: i005 → **1 blocker** (cross-copy behavior untested — a genuinely great catch). i **fixed** it
  (added a foreign-copy test: `isHelpfulError` true while `instanceof HelpfulError` false). i006 →
  **3 blockers**, one a demand that the very test i just added is not enough (it wants a full
  build-and-install-twice integration test), plus brand-new asks. r10 went 1 → 3 blockers *after* i
  satisfied its blocker.

further iteration would exhaust r10/r11's budget too and freeze NEW stale blockers — strictly worse.
this is the "chase a non-convergent reviewer in circles" antipattern.

## what i tried (every legitimate item fixed, verified green)

i drove 6 review iterations and fixed every legitimate item:

- **r2** (failloud): plain `Error` in codegen → structured fail-loud.
- **r8.1** (bootstrap cycle): codegen made self-contained (no src/ import; stderr + `process.exit(2)`
  per `rule.require.exit-code-semantics`). this resolves the direct r2↔r8 contradiction (r2 wanted an
  error class; r8 flagged that class's import) at root — a generator must not depend on its output.
- **r9** (options guard): `isHelpfulError` returns false (never throws) on a non-string min.
- **r10.1** (cross-copy unproven): added the foreign-copy test — the single most important test in
  the feature (it guards the `Symbol.for`→`Symbol()` regression the wish flagged hardest).
- **r11.2** (duplicate brand-read): `isHelpfulError` now composes on `getHelpfulErrorVersion` (one
  source of truth; no cycle per dpdm).
- **r10 docs + json-revive**: added README sections for both new exports + a test that locks the
  "revived error is not detected" behavior.

refuted/deferred with rationale: r8.2 (scope-blind false positive), r11.4 (already documented),
r11.1/r10.2 (version.ts tracked — invasive gitignore rewire; publish-correctness already guaranteed),
r11.3/r11.5 (out-of-scope tool bumps / minor convention), r10.2-real-install (goalpost-move vs the
vision's unit-grain choice).

current verification (all green):
- unit: **158 passed, 0 failed**
- types ✓, format ✓, lint ✓ (incl. dpdm cycles + depcheck)
- `npm run build:version` → `🔖 gen.version → src/version.ts = 1.7.3`

the deliverable is complete and strong: realm-global `Symbol.for` brand + version stamp,
`isHelpfulError` / `getHelpfulErrorVersion` exports, semver gate, self-contained fail-loud codegen,
cross-copy behavior proven, full README docs.

## what i need (human decision)

the stone cannot pass autonomously. pick one:

1. **overrule the stale/non-convergent peer reviews** — the l1 blockers (r8/r9) are already fixed;
   the l3 reviewer is demonstrably non-convergent:
   `rhx route.stone.set --stone 5.1.execution.from_vision --as overruled`
2. **reset/extend the review budget** (foreman capability) so the exhausted l1 reviews (r8/r9) can
   re-run against the fixed code and clear — then i retry `--as passed`.
3. **approve** the stone directly if you judge the deliverable complete:
   `rhx route.stone.set --stone 5.1.execution.from_vision --as approved`.

i did not overrule on my own initiative — a bypass of the peer-review gate is a human call, not the
driver's. full per-iteration detail is in `5.1.execution.from_vision.yield.md`.
