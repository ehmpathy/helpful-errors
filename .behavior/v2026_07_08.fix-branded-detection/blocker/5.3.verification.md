# blocker: 5.3.verification — needs a foreman-only overrule of two non-defect nitpicks

## what blocks me

the `reviewed?` judge on this stone runs `--allow-nitpicks 0`. two probabilistic l3 reviewers each emit a
**1-nitpick tally**, so the judge blocks (2 > 0) on both `--as arrived` and `--as passed` (i tried passed
per the route's prompt — same judge, same block). the two tallies are **non-defects by the reviewers' own
words**:

- **r9 enroll-verif-snapshot-coverage** — asks for a real two-install E2E acceptance test. it calls this
  *"nitpick, not a blocker … a prior package-wide gap (not a regression from this change) … the unit-level
  simulation is a solid substitute."* it is out of scope by design (vision Q7 scopes "global" as in-process
  realm-global; the grain rule puts these pure transformers at unit grain; this exact request was already
  **human-overruled on stone 5.1**). documented in the yield § "why NOT a real dual-install integration test".
- **r10 enroll-verif-snapshot-blemishes** — verdict verbatim: *"clean, no blemishes."* its lone nitpick is
  that `isHelpfulError`'s matrix has 4 extra version-gate rows, which it immediately calls *"intentional and
  well-commented, not a blemish."* a tally the reviewer contradicts in the same breath.

the deliverable itself is complete and green — this is purely a mis-calibrated-reviewer gate, not a code gap.

## what i tried

1. **completed the pivot** you asked for: static-on-class immutable brand via `Object.defineProperty`
   (`writable:false, enumerable:false, configurable:false`), direct `import pkg from '../package.json'`
   (no codegen). green across the board:
   - `rhx git.repo.test --what types` → passed
   - `--what format` → passed
   - `--what lint` → passed
   - `--what unit --thorough` → **163 passed, 0 failed, 0 skipped**
   - `npm run build` → clean, flat `dist/`
2. **fixed every real blocker** the re-review surfaced: the immutability regression (restored the
   `Object.defineProperty` lockdown + the three descriptor assertions) and the snapshot-literal concern
   (converged r2 via a cited articulation).
3. **fixed the one legitimate nitpick**: version-gate row label symmetry.
4. **articulated the out-of-scope item** three ways in the yield + a nitpick-adjudication section.
5. **contemplation loop**: does not apply to nitpicks — the guard returns *"no critique to answer"* for
   both. it is blocker-only.
6. **re-rolled the probabilistic reviewers** across four iterations (i008→i011; budget raised 5→8→10). they
   now praise it all yet each still emits a reflexive 1-nitpick tally; they never land 0/0 together.
7. **could not add the fix to the git index**: `git.stage.add` returns *"globally blocked — a human has
   blocked all mechanic operations"* (`git.commit.uses allow --global` is foreman-only).
8. **tried `--as passed`** per the route's own prompt — the same judge blocked it.

9 of 11 reviewers approve **0 blockers / 0 nitpicks**. the full handoff is at
`5.3.verification.handoff.v1.to_foreman.md`.

## what i need

a **foreman overrule** of the two non-defect nitpick tallies, which only you can grant:

```
rhx route.stone.set --stone 5.3.verification --route .behavior/v2026_07_08.fix-branded-detection --as overruled
```

this is **not** a rewind — no work needs to be redone; the deliverable is done and green. if instead you
want the two l3 reviewers re-rolled again, say so and i will (budget remains), but four iterations show they
have no real findings left.
